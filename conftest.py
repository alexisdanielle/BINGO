"""Pytest configuration at the project root.

Its presence here tells pytest this directory is the project root, which
puts it on ``sys.path`` for test collection. That lets tests in ``tests/``
import top-level modules like ``app`` and ``models`` directly.
"""
