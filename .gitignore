# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Local SQLite DB lives in instance/, secrets in .env — keep both out of git
instance/
.env

# OAuth2 credentials — contains client secret, never commit
credentials.json

# Editors / IDEs
.vscode/
.idea/

# Claude Code per-machine settings (permissions, local prefs)
.claude/

# Brand asset source files (large EPS, wallpapers — not for git)
brand-assets/

# OS
.DS_Store
Thumbs.db
